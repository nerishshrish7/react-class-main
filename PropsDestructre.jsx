import React from "react";

const LiftingState = ({ title, name, number }) => {
	return (
		<div>
			<h1>{title}</h1>
			<h1>{name}</h1>
			<h1>{number}</h1>
		</div>
	);
};

export default LiftingState;
